import { Metadata } from "next";

import { HomePage } from "pages-flat";

export const metadata: Metadata = {
  title: "Bebrotype",
  description: "Blindtyping trainer for real bebrochads",
  icons: "/icons/banana.svg",
};

export default HomePage;
