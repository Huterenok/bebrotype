import { FC } from "react";

import { IText } from "../../types";

import { Card } from "shared/ui";

export const TextCard: FC<IText> = ({ id, content, likes, title }) => {
  return <Card>bebra</Card>;
};
